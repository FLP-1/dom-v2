{
  "name": "dom-v2-frontend",
  "version": "2.0.0",
  "private": true,
  "description": "DOM v2 - Sistema de Gestão Doméstica Familiar (HTML Nativo)",
  "main": "public/index.html",
  "scripts": {
    "start": "npx serve public -p 3000",
    "dev": "npx serve public -p 3000",
    "build": "echo 'Build não necessário para HTML nativo'",
    "serve": "npx serve public -p 3000",
    "validate-html": "npx html-validate public/**/*.html",
    "validate-architecture": "node scripts/validate-architecture.js",
    "lint": "echo 'Linting não necessário para HTML nativo'",
    "test": "echo 'Testes serão implementados conforme necessário'",
    "clean": "echo 'Limpeza concluída'"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "devDependencies": {
    "html-validate": "^8.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dom-v2/frontend.git"
  },
  "keywords": [
    "html",
    "css",
    "javascript",
    "domestic-management",
    "family-system"
  ],
  "author": "DOM v2 Team",
  "license": "MIT"
}
