import { describe, test, expect, beforeAll, afterAll } from '@jest/globals';

describe('setup', () => {
  beforeAll(async () => {});
  afterAll(async () => {});
  test('setup smoke', () => expect(true).toBe(true));
});
